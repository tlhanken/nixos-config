# Tasks: 001-openspec-agent-workflow

- [x] Initialize OpenSpec configuration (`openspec/config.yaml`)
- [x] Write domain specifications in `openspec/specs/` (`core-architecture`, `hosts-and-hardware`, `apps-and-services`, `home-manager-profiles`, `networking-and-storage`, `secrets-and-security`, `nixos-config`)
- [x] Update repository agent guidelines (`AGENTS.md`)
- [x] Refine specifications to represent intent and architectural contracts rather than implementation details
- [x] Validate OpenSpec directory structure and change proposal workflow
